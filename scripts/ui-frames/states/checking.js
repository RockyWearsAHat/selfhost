/* Boot: neither answer has landed yet, so no view is shown at all. */
window.__STATE = {
  "routes": {
    "GET /capabilities": {
      "pending": true
    },
    "GET /me": {
      "pending": true
    }
  }
};
