[package]
name = "selfhost-proxy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "TLS termination, static serving with byte ranges, reverse proxy, and health-checked load balancing."

[dependencies]
selfhost-http = { workspace = true }
selfhost-config = { workspace = true }
# EWS/ActiveSync/Autodiscover need the same Maildir store and credential
# check IMAP/submission already use — see `Server::attach_mail` for why this
# is the one process's single shared handle rather than a second store.
selfhost-mail = { workspace = true }
# The webhook route reads the daemon's service catalogue (to find a watch's
# secret) and calls its loopback control API (to trigger a deploy) — both
# already-built, already-tested pieces of `selfhost-admin` rather than a
# second copy of either.
selfhost-admin = { workspace = true }
tokio = { workspace = true }
tokio-rustls = { workspace = true }
rustls = { workspace = true }
rustls-pemfile = { workspace = true }
rcgen = { workspace = true }
# HMAC-SHA256 for verifying a webhook's signature. Not a second crypto
# provider: `ring` is the one this workspace already links, for TLS.
ring = { workspace = true }
# One symbol only: `looks_like_upgrade`, the workspace's single definition of
# "is this request head a WebSocket handshake?". Linking the crate does not run
# any of it — see the containment note at the top of `src/upgrade.rs` for why
# the frame codec must never execute in this process, and why a pure header
# predicate is not that.
selfhost-ws = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
