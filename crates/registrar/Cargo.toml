[package]
name = "selfhost-registrar"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Pushes the deployment's DNS records, derived from the config, to the registrar hosting the domain's zone."

[dependencies]
selfhost-config = { workspace = true }
selfhost-json = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
