[package]
name = "selfhost-http"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "HTTP/1.1 message parsing and serialisation. No dependencies: everything on the wire is ours."

[dependencies]

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
